#include "square.h"   // double quotes used instead of <> when the header file is in the same directory as the source


// definition (implementation) of functions declared in square.h come here


int square(int x) {
  return x * x;
}

